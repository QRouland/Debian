#!/bin/bash
echo
echo "Start -- SSHinstall -- Start"

echo -n "Intallation SSH :" 
exec apt-get-wait.sh
apt-get install -y openssh-server &>/dev/null

if [ $? -eq 0 ]; then 
	echo "OK"
else
	echo "NOK"
fi

echo "End -- SSHinstall -- End"
echo
